[tool.poetry]
name = "dronefly"
version = "0.2.0"
description = "Chat platform software for naturalist-related sites"
authors = ["Ben Armstrong <synrg@debian.org>"]
license = "AGPL-3.0-or-later"
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.8.1,<3.10"
Red-DiscordBot = { git = "https://github.com/Cog-Creators/Red-DiscordBot", branch = "V3/develop" }
pyinaturalist = "^0.17.0"
pytest-mock = "^3.6.1"
dateparser = "^1.1.1"

[tool.poetry.group.dev.dependencies]
black = "^21.8-beta.0"
pytest = "^6.2.5"
pylint = "^2.10.2"
pytest-asyncio = "^0.16.0"
pre-commit = "^2.18.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
